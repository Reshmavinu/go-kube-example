# Ingress resource for the application
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: go-kube-example
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - host: go-kube-example.local
    http:
      paths: 
      - path: /
        pathType: Prefix
        backend:
          service:
            name: go-kube-example
            port:
              number: 80
